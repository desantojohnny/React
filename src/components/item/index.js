const Item = /* (props) */ ({children}) => {
    return (
        <a href="/" className="list-group-item list-group-item-dark">
        {/* {props.texto}  */ children}
        </a>
    )
}

export default Item;